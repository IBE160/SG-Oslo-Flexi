# AI Buddy Backend

This is the backend service for AI Buddy, built with FastAPI and Poetry.

## Setup

1. Install dependencies:
   ```bash
   poetry install
   ```

2. Run the server:
   ```bash
   poetry run uvicorn app.main:app --reload
   ```
