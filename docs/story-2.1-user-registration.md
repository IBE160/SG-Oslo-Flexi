# Story 2.1: User Registration

**Status:** done

## Story

**As a new user,** I want to be able to register for an account using my email and a password, so that I can access the application.

## Acceptance Criteria

*   Given I am on the registration page,
*   When I enter my email and a valid password,
*   Then my account is created.
*   And I am redirected to the login page.

## Dev Notes

### References

*   [Source: docs/tech-spec-epic-2.md#story-21-user-registration]
*   [Source: docs/epics.md#epic-2-user-authentication--onboarding]
*   [Source: docs/architecture.md#5-project-structure]
*   [Source: docs/architecture.md#9-novel-patterns]

### Architecture patterns and constraints

The implementation should follow the REST API patterns and database naming conventions outlined in the architecture document. The backend endpoint will be part of the main FastAPI application and not a separate agent.

### Learnings from Previous Story
N/A. This is the first story in the epic.

## Tasks

### Backend

*   [x] (AC: #1) Create `POST /api/v1/users/register` endpoint.
*   [x] (AC: #1) Implement request body validation for email and password.
*   [x] (AC: #1) Hash password using bcrypt before storing in the `users` table.
*   [x] (AC: #1) Return `201 Created` with user ID and email on success.
*   [x] (AC: #1) Return `400 Bad Request` or `409 Conflict` on error.
*   [x] (AC: #1) **Testing:** Write unit tests for the registration endpoint, covering success and error cases.

### Frontend

*   [x] (AC: #2) Create `RegistrationForm` component.
*   [x] (AC: #2) Manage form state for email, password, and submission status.
*   [x] (AC: #2) Create API service function to call the registration endpoint.
*   [x] (AC: #2) On successful registration, redirect the user to the `/login` page.
*   [x] (AC: #2) **Testing:** Write unit tests for the `RegistrationForm` component, mocking the API service.

## Dev Agent Record

*   **Context Reference:** Story 2.1 Implementation
*   **Agent Model Used:** gemini-2.0-flash-exp
*   **Debug Log References:**
    *   Dependency issues with `passlib` 1.7.4 and `bcrypt` 4.0+ (downgraded `bcrypt` to 3.2.2).
    *   Missing `email-validator` for Pydantic.
    *   Added `aiosqlite` for in-memory DB testing.
*   **Completion Notes List:**
    *   Implemented full backend stack (Model, Schema, Service, API, Security).
    *   Implemented frontend registration form and API client.
    *   Verified backend functionality with `backend/tests/validate_story_2_1.py`.
*   **File List:**
    *   `backend/app/core/security.py`
    *   `backend/app/schemas/user.py`
    *   `backend/app/services/user.py`
    *   `backend/app/api/users.py`
    *   `backend/tests/api/test_users.py`
    *   `backend/tests/validate_story_2_1.py`
    *   `frontend/src/lib/api.ts`
    *   `frontend/src/components/RegistrationForm.tsx`
    *   `frontend/src/app/register/page.tsx`
    *   `backend/pyproject.toml` (Updated dependencies)

## Change Log

*   2025-12-07: Initial draft generated by validation workflow auto-correction.
*   2025-12-07: Implementation completed and validated by Dev Agent.
